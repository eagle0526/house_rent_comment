<h2>使用者新增的房屋</h2>


<%= link_to "新增房屋", new_admin_user_house_path(current_user), class: "btn-primary" %>

<br>


<section>
  <div>
    <h3 class="mb-4 text-center">你新增的所有房屋</h3>
  </div>

  <table class="w-full text-base text-left text-gray-500 dark:text-gray-400">
      <thead class="text-base text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
      
      <tr>
          <th class="px-6 py-3">id</th>
          <th class="px-6 py-3">房屋名稱</th>
          <th class="px-6 py-3">房屋描述</th>                    
          <th class="px-6 py-3">圖片</th>          
          <th class="px-6 py-3">刪除房屋</th>
      </tr>
      </thead>
      
      
      <tbody>
      <% @houses.each do |house| %>
      <tr>
          <td class="px-6 py-4"><%= link_to house.id, admin_house_path(house), class: "btn-primary" %></td>
          <td class="px-6 py-4"><%= house.title %></td>
          <td class="px-6 py-4"><%= house.description %></td>
          <% if house.images.first %>
            <td class="px-6 py-4"><%= image_tag house.images.first %></td>
          <% end %>
          <td class="px-6 py-4"><%= link_to "刪除房屋", admin_house_path(house.id), data: { turbo_method: "delete", confirm: "確定嗎？" }, class: "second-btn" %></td>

      </tr>
      <% end %>  
      </tbody>
      
  </table>


</section>
